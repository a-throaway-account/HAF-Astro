<!doctype html>
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Hustle and Flow</title>
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
    <link rel="icon" href={`${import.meta.env.BASE_URL}/favicon.ico`}/>
    <link rel="stylesheet" href={`${import.meta.env.BASE_URL}/css/styles.css`}>
     <link rel="stylesheet" href={`${import.meta.env.BASE_URL}/css/focal-hero.css`}>     
     <link rel="stylesheet" href={`${import.meta.env.BASE_URL}/css/philosophy.css`}>
     <link rel="stylesheet" href={`${import.meta.env.BASE_URL}/css/services.css`}>
     <link rel="stylesheet" href={`${import.meta.env.BASE_URL}/css/blogs.css`}>
  	 <link rel="stylesheet" href={`${import.meta.env.BASE_URL}/css/marquee.css`}>
    <link  href="https://assets.calendly.com/assets/external/widget.css"
  rel="stylesheet"/>
         

    <style>
      html, body {
        margin: 0;
        width: 100%;
        height: 100%;
      }
    </style>
  </head>
  <body>
    <slot />

  </body>

</html>
