<section id="our-services" class="services-section">

  <div class="container">

    <!-- HEADER -->
    <div class="services-header">
      <div class="header-left">
        <span class="label-accent">Our Expertise</span>
        <h2 class="section-title">
          We got you Covered,<br>
          <span class="text-highlight-htt">Head to Toe.</span>
        </h2>
      </div>
    </div>

    <!-- SLIDER -->
    <div class="services-slider">

      <!-- LEFT ARROW -->
      <button class="slider-arrow prev" id="prevBtn"
        onclick="moveServices('left')">
        <svg width="20" height="20" viewBox="0 0 24 24"
          fill="none" stroke="currentColor"
          stroke-width="2.5" stroke-linecap="round"
          stroke-linejoin="round">
          <path d="m15 18-6-6 6-6"/>
        </svg>
      </button>

      <!-- RIGHT ARROW -->
      <button class="slider-arrow next" id="nextBtn"
        onclick="moveServices('right')">
        <svg width="20" height="20" viewBox="0 0 24 24"
          fill="none" stroke="currentColor"
          stroke-width="2.5" stroke-linecap="round"
          stroke-linejoin="round">
          <path d="m9 18 6-6-6-6"/>
        </svg>
      </button>

      <!-- TRACK -->
      <div class="services-track" id="servicesGrid">

        <!-- CARD -->


<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">01</div>
    <img src="logos/services/meta.png" class="service-gif" />
  </div>
  <h5>Meta <span class="accent-text">Ads</span></h5>
  <p>High-converting Facebook and Instagram campaigns built for scalable ROAS.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">02</div>
    <img src="logos/services/ga.jpg" class="service-gif" />
  </div>
  <h5>Google <span class="accent-text">Ads</span></h5>
  <p>Capture high-intent demand through search, shopping, display, and YouTube.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">03</div>
    <img src="logos/services/seo.jpg" class="service-gif" />
  </div>
  <h5>SEO <span class="accent-text">Optimization</span></h5>
  <p>Long-term organic growth powered by technical, on-page, and content SEO.</p>
</div>
        <div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">04</div>
    <img src="logos/vids/amazon-animation.gif" class="service-gif" />
  </div>
  <h5>Amazon <span class="accent-text">Ads</span></h5>
  <p>Launch and scale your D2C brand on India’s leading marketplace.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">05</div>
    <img src="logos/vids/amazon-animation.gif" class="service-gif" />
  </div>
  <h5>Amazon <span class="accent-text">Marketplace</span></h5>
  <p>Listing optimization, A+ content, storefronts, and full account management.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">06</div>
    <img src="logos/services/flipkart.png" class="service-gif" />
  </div>
  <h5>Flipkart <span class="accent-text">Ads</span></h5>
  <p>Marketplace-specific strategies to dominate visibility and conversions.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">07</div>
    <img src="logos/services/growth marketing.png" class="service-gif" />
  </div>
  <h5>Growth <span class="accent-text">Marketing</span></h5>
  <p>Full-funnel experimentation across acquisition, activation, and retention.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">08</div>
    <img src="logos/services/yt.png" class="service-gif" />
  </div>
  <h5>YouTube <span class="accent-text">Ads</span></h5>
  <p>Video-first campaigns optimized for awareness, consideration, and scale.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">09</div>
    <img src="logos/services/retention.png" class="service-gif" />
  </div>
  <h5>Retention <span class="accent-text">Marketing</span></h5>
  <p>Email, WhatsApp, SMS, and lifecycle flows to maximize LTV.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">10</div>
    <img src="logos/services/GA.png" class="service-gif" />
  </div>
  <h5>Analytics & <span class="accent-text">Reporting</span></h5>
  <p>Custom dashboards, GA4, attribution modeling, and performance insights.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">11</div>
    <img src="logos/services/mark automation.jpg" class="service-gif" />
  </div>
  <h5>Marketing <span class="accent-text">Automation</span></h5>
  <p>CRM workflows, integrations, lead scoring, and funnel automation systems.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">12</div>
    <img src="logos/vids/webdev-animation.gif" class="service-gif" />
  </div>
  <h5>Web <span class="accent-text">Development</span></h5>
  <p>High-performance websites optimized for UX, speed, and conversions.</p>
</div>

<div class="service-card">
  <div class="service-icon-row">
    <div class="service-icon">13</div>
    <img src="logos/services/creative design.png" class="service-gif" />
  </div>
  <h5>Creative <span class="accent-text">Design Analysis</span></h5>
  <p>Ad creative diagnostics, CRO insights, and performance-led design direction.</p>
</div>

      </div>
    </div>

  </div>
</section>


<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    let serviceIndex = 0;
    let isMoving = false;

    const track = document.getElementById("servicesGrid");
    if (!track) return;

    const cards = Array.from(track.children);
    if (cards.length === 0) return;

    // 1. Clone cards for infinite effect
    cards.forEach(card => track.appendChild(card.cloneNode(true)));
    cards.slice().reverse().forEach(card => track.prepend(card.cloneNode(true)));

    const totalCards = track.children.length;
    const realCardCount = cards.length;
    
    // Calculate width including the gap/margin (adjust 30 to match your CSS gap)
    const cardWidth = cards[0].offsetWidth + 30;

    // 2. Set initial position to the "real" first card
    serviceIndex = realCardCount;
    track.style.transition = "none";
    track.style.transform = `translateX(-${serviceIndex * cardWidth}px)`;

    // 3. Attach function to window so the onclick="moveServices()" can find it
    window.moveServices = (direction) => {
      if (isMoving) return;
      isMoving = true;

      if (direction === "right") {
        serviceIndex++;
      } else {
        serviceIndex--;
      }

      track.style.transition = "transform 0.45s cubic-bezier(0.4, 0, 0.2, 1)";
      track.style.transform = `translateX(-${serviceIndex * cardWidth}px)`;

      // 4. Teleport Logic (The "Infinite" Snap)
      setTimeout(() => {
        track.style.transition = "none";

        // If we reach the end of the clones on the right
        if (serviceIndex >= realCardCount * 2) {
          serviceIndex = realCardCount;
          track.style.transform = `translateX(-${serviceIndex * cardWidth}px)`;
        }

        // If we reach the clones on the left
        if (serviceIndex <= 0) {
          serviceIndex = realCardCount;
          track.style.transform = `translateX(-${serviceIndex * cardWidth}px)`;
        }
        
        isMoving = false;
      }, 460);
    };
  });
</script>